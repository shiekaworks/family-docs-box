import React from 'react';
import { poppins } from '@/app/_assets/fonts';

export const Sidebar = (): React.JSX.Element => {
  return (
    <div
      className={`w-[20vw] py-[35px] px-[30px] rounded-2xl bg-black ${poppins.className}`}
    >
      jasjahsaj
    </div>
  );
};
